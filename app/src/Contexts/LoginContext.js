import { createContext } from "react";

//context is passed in from App.js via LoginContext.Provider values
//from here, it can be used anywhere in the app

export const LoginContext = createContext({});